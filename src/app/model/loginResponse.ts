export default class LoginResponse {
    // constructor(statusCode: number, name: string, username: string, role: string, enabled: boolean, token: string) { }
    statusCode: number = 0;
    name: string = '';
    username: string = '';
    role: string = '';
    enabled: boolean = true;
    token: string = '';
}